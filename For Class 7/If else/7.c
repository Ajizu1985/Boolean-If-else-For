#include <stdio.h>
#include <math.h>

int main (){

    int first=6, second=-3;
    if (first < second) {
        printf("1\n");
    }
    if (first > second) {
        printf("2\n");
    }
    return 0;
}