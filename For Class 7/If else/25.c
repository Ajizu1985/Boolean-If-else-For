#include <stdio.h>
#include <math.h> 
   int main (){
   float num1=1;
   
    if (num1 < -2 || num1 > 2 ) {
        printf("%f\n", 2*num1);
        }
   else    
 {
        printf("%f\n", -3*num1);
        }
    return 0;
}