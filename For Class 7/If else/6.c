#include <stdio.h>
#include <math.h>

int main (){

    int num1=-2, num2=0;
    if (num1 > num2) {
        printf("%d\n", num1);
    }
    if (num1 < num2) {
        printf("%d\n", num2);
    }
    return 0;
}
